# Name: Process Pixels
# Author: The Fiji Team
# Version: 1.0.0

# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins>Analyze, "Remove Histogram Gaps", fiji.plugins.small.RemoveHistGaps
Plugins>Analyze, "Process Pixel", fiji.plugins.small.Process_Pixels
Plugins>Analyze, "Tubeness Stack", fiji.plugins.small.TubenessStack
Plugins>Analyze, "MySkeleton", fiji.plugins.small.MySkeleton
